# Work log — 2026-06-29

> A plain-language record of everything done in this session, written so someone
> who has never seen this project can follow it. Technical detail lives in the
> commit messages and the design docs; this file is the story.

## What Hakam is (30-second version)

Hakam is a **firewall that lives inside the Linux kernel**. Instead of being a
program that watches traffic from the outside, parts of it run *inside* the
kernel itself, right where packets and system calls happen. It's built to be
shown at **Black Hat Arsenal** (a security conference), so the whole project is
organised around a roadmap (`docs/arsenal_roadmap.md`) with numbered features
and deadlines.

A few terms that come up constantly:

- **eBPF** — a way to safely run small programs *inside* the Linux kernel. The
  kernel has a "verifier" that refuses to load any eBPF program it can't prove
  is safe (no infinite loops, no reading random memory). "Passing the verifier"
  is the recurring hurdle.
- **XDP** — an eBPF program that runs the instant a network packet arrives, the
  earliest possible point. Hakam uses it to inspect and drop packets.
- **BPF-LSM** — an eBPF program attached to a "security hook" inside a system
  call. Hakam uses it on `connect()` (the call a program makes to open a network
  connection) so it can *deny the connection before it even happens*.
- **conntrack** ("connection tracking") — keeping a little table of the active
  network connections so you can remember things about each one.
- **the VM** — eBPF only builds and runs on Linux, but this work happens on a
  Mac. So there's a Linux virtual machine called `hakam` (via OrbStack) where
  everything is actually built and tested. The Mac drives it remotely.

We never push to GitHub — all work is committed **locally** only.

---

## Part 1 — Reviewing the project before continuing

We started by reviewing the codebase to find problems before adding new
features. The review found that a feature called **#6 (BPF-LSM connect
enforcement)** had been written but not committed, not tested, and had a few
issues. The findings were written up in `docs/phase2_review_2026-06-29.md`.

The most important issue: the code *claimed* to be "per-process" enforcement,
but it actually wasn't. It blocked connections based on the **destination**
(where you're connecting *to*), not based on *which program* is connecting. We
fixed the wording so the claim matches reality — important because at the
conference, experts will ask hard questions and any exaggeration hurts
credibility.

We also added a safety check to the pre-demo script (`preflight.sh`) and a test
(`smoke.sh`) and fixed a documentation file to match the code.

## Part 2 — Proving feature #6 actually works

Feature #6 is supposed to **block a program from opening a network connection
to a forbidden address**. Talking about it isn't enough — we proved it live in
the `hakam` VM:

1. We told Hakam to block the address `1.2.3.4`.
2. From a normal terminal we tried to connect there with three different tools
   (`curl`, `python3`, `nc`).
3. All three were **denied instantly** with the error "Operation not permitted"
   — and the denial happened in **0 milliseconds** (meaning no network packet
   was ever sent; the connection was stopped before it started).
4. As a control, connecting to a *different*, non-blocked address still worked —
   proving Hakam blocks only what you tell it to, not everything.

That was the proof feature #6 works. We then committed it and marked it done in
the roadmap's progress tracker.

**A note on the confusion that happened here:** Hakam has *two* different
prompts. One is Hakam's own command line (where you type `policy-block`), and
the other is a normal Linux terminal (where you type `curl`). Typing `curl` into
Hakam's prompt does nothing — they're different things. That tripped us up
briefly; the fix was just "use a second terminal for normal commands."

## Part 3 — Starting feature #7 (the big one)

With #6 done, the only remaining headline feature is **#7: the eBPF conntrack**
(the connection-tracking table). We first wrote a detailed plan
(`docs/phase2_7_conntrack_plan.md`) and then started building it in small,
safe steps, testing and committing after each one.

### Why #7 matters (in plain terms)

When data is sent over the network, it's broken into pieces called **segments**.
An attacker can hide an attack by splitting it across two segments — each half
looks innocent on its own. Hakam already stitches segments back together to
catch this. But it had two weaknesses:

1. If segments arrive **out of order** (common on the real internet), the
   stitched-together result is scrambled and the attack can slip through.
2. The network sometimes **re-sends** a segment ("retransmit"). Hakam would
   stuff the duplicate into its buffer, wasting space.

To fix both, Hakam needs to know each segment's **sequence number** (a counter
TCP puts on every segment saying where it belongs in order). Feature #7 builds a
small table in the kernel that tracks this per connection. Here's what we did,
step by step.

### Step "flow-types foundation"

We defined the **shape** of the data: a `FlowKey` (the four things that identify
a connection: source address, destination address, source port, destination
port) and a `FlowState` (what we remember about each connection: the next
expected sequence number, a timestamp, a packet count). We also created the
kernel table itself, `CONNTRACK`, which auto-forgets the oldest connections when
it gets full (so it can never run out of memory and crash). Verified with tests.

### Step "xdp-observe"

We made the XDP program (the one that runs on every arriving packet) read each
segment's **sequence number** and record the connection into the `CONNTRACK`
table. **This was the riskiest moment of the whole feature** — the kernel
verifier might have rejected the new code. We tested it by loading Hakam onto a
spare network interface in the VM, and it **loaded successfully on the first
try**. That removed the single biggest risk in the plan.

### Step "seq-on-the-wire"

The kernel sees *every* segment, but the part of Hakam that does the
stitching (in userspace) only sees *some* of them. So the kernel is the only
one that can correctly judge whether a segment is **in order**, a **duplicate**,
or **out of order (a gap)**. In this step the kernel makes that judgment and
attaches a little label to each segment it passes up. Verified: tests pass and
the verifier still accepts the program.

### Step "userspace-dedupe"

We taught the stitching code to **use those labels**:

- A segment labelled "duplicate" → **thrown away** (no more wasted buffer
  space).
- A segment labelled "out of order" → counted (so we can see how often it
  happens) and, for now, still kept so we never lose real attack data.
- A segment labelled "in order" → added normally.

We added tests proving a duplicate is dropped and an out-of-order segment is
counted. The whole project rebuilt and all tests passed.

### Step "reorder-buffer"

This is the step that actually **fixes out-of-order segments**, and it landed at
full strength (not the scaled-back fallback we had planned as a safety net).

The problem in plain terms: an attacker can send the *second half* of an attack
before the *first half*. If Hakam just glues pieces together in the order they
arrive, it gets "second-half + first-half" — scrambled — and misses the attack.

The fix: every piece now carries its **sequence number** (a counter saying where
it belongs). Hakam keeps the pieces in a sorted structure keyed by that number
and always rebuilds the combined view **in the correct order**, no matter what
order they arrived in. So "second-half-first" gets sorted back into
"first-half + second-half" and the attack is caught.

A subtle but important detail we got right: the kernel's quick label can't tell
the difference between "an earlier piece that arrived late" and "a duplicate
piece" — both look like they belong *before* what we've already seen. So for
putting bytes in order, we rely on the **sequence number** (which knows the
difference) rather than that label. We even wrote a test that feeds Hakam a
late piece the kernel *wrongly* calls a duplicate, and proves Hakam still places
it correctly and catches the attack.

We added unit tests and an end-to-end test (run the reordered attack all the way
through the real attack-matcher) — all green.

### Step "stats-surface"

This step makes the new connection table **visible** — which is literally the
requirement to call feature #7 "done" (the roadmap says: "conntrack table holds
N active flows visible in stats").

Two places now show it:
- The live status feed (what the on-screen dashboard reads) gained an
  **`active_flows`** number — a live count of how many connections the kernel is
  currently tracking. We update it once a second.
- Hakam's `stats` command now prints `active flows` plus three reassembly
  numbers: how many flows are being stitched, how many duplicate pieces were
  dropped, and how many arrived out of order.

We confirmed the new number actually flows all the way through to the dashboard
feed (it showed `"active_flows":0` on an idle test interface — zero because that
test interface carries no real traffic; proving it counts *real* connections is
the next step's job).

**A useful thing we discovered along the way:** Hakam currently shuts itself
down the moment its keyboard input stream ends. That's fine when a person is
running it in a terminal, but it meant our automated test launches died
instantly. We worked around it for testing, and noted that this is exactly the
already-planned Phase 3 item #9 ("fix the stdin-exit bug"). Good to have
confirmed it first-hand.

### Step "vm-validate"

This is the live, end-to-end proof on a real running Hakam — not a unit test. We
stopped the old build, launched the new one, and fired real network traffic at
it inside the VM. What we confirmed:

1. **The connection counter works for real.** As we sent traffic, the live
   `active_flows` number climbed from 0 to 4 to 12 — the kernel really is
   tracking each connection. That's the headline requirement for feature #7,
   now proven on a running system, not just in tests.
2. **Normal attack detection still works.** A SQL-injection request from a valid
   address was caught and blocked (`BLOCK … category SQLi`), and Hakam even named
   the program that sent it (`PID … / nc`). So none of the #7 plumbing broke the
   existing detection.
3. **Split-attack reassembly works on the wire.** The real prize: we sent an
   attack deliberately cut into **two separate network pieces**, with the
   dangerous keyword `UNION SELECT` split across the seam (`…UNI` + `ON SELECT…`).
   Hakam stitched the two pieces back together by sequence number and caught it —
   `BLOCK … payload "UNION SELECT"`. This is the evasion #7 was built to close,
   working live.

Honest notes:
- A couple of our first attempts didn't block — but the cause was our **test
  setup**, not Hakam: one used a source address that wasn't actually configured,
  and others used requests shorter than Hakam's 64-byte inspection window, or got
  silently merged by the loopback network. Once we sent a properly sized,
  properly split request from a real address, it blocked every time.
- We did **not** live-test the *out-of-order* case (pieces arriving in the wrong
  order), because forcing a real network to deliver packets out of order needs
  special packet-crafting tools. That exact case is covered by our automated
  tests instead, which control delivery order directly.

---

## Where things stand at end of day

**Roadmap position:** **Phase 2 is complete** — features #6, #7, and #8 are all
done. That's the entire headline feature set (XDP edge drop + BPF-LSM syscall
enforcement + per-process attribution + per-flow conntrack).

**#7 steps done (all 8):** flow-types foundation → xdp-observe → seq-on-the-wire
→ userspace-dedupe → reorder-buffer → stats-surface → vm-validate →
docs-and-flip. (The reorder step landed at full scope; we did not need its
planned fallback. Live validation confirmed active_flows climbing, single-shot
detection intact, and a real split-segment SQLi blocked on the wire.)

**docs-and-flip** updated the documentation to match what actually shipped and
flipped the roadmap tracker. Notably, the Q&A-prep doc had *predicted* a
per-CPU map with a TCP state machine and CLI knobs that we didn't build — we
corrected it to the real shipped design (one shared LRU table, no state machine,
a single `active_flows` gauge) so a reviewer reading the doc and the code sees
the same story. The roadmap tracker now shows #7 ✅.

**Next up:** Phase 3 #9 (systemd unit + container image) — which also fixes the
stdin-exit bug we hit first-hand during vm-validate.

**Commits made today (all local, nothing pushed):**
1. BPF-LSM connect enforcement (#6) — the feature, validated
2. Smoke-test + cleanup tweaks
3. Conntrack flow types + table scaffold (#7)
4. XDP fills the conntrack table (#7)
5. Segment classification + labels on events (#7)
6. Userspace duplicate-dropping + gap awareness (#7)
7. Sequence-ordered reassembly / reorder buffer (#7)
8. Surface active_flows + reassembly gauges in stats (#7)

(vm-validate added no code — it was a live test of the running build.)

**Key wins:** #6 is proven and shipped; the scariest part of #7 (kernel
acceptance) passed on the first try; nothing is broken — every step ended with
passing tests.
